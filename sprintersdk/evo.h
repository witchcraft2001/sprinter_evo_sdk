#pragma disable_warning 85

// Sprinter port of EvoSDK -- API contract IDENTICAL to evosdk/evo.h.
// Не менять сигнатуры/семантику без согласования (CLAUDE.md §2.2).

//определения типов переменных

typedef unsigned char u8;
typedef   signed char i8;
typedef unsigned  int u16;
typedef   signed  int i16;
typedef unsigned long u32;
typedef   signed long i32;

#define TRUE	1
#define FALSE	0

//флаги кнопок джойстика

#define JOY_RIGHT	0x01
#define JOY_LEFT	0x02
#define JOY_DOWN	0x04
#define JOY_UP		0x08
#define JOY_FIRE	0x10

//смещения в массиве клавиш для опроса клавиатуры

#define KEY_SPACE	0x23
#define KEY_ENTER	0x1e
#define KEY_SYMBOL	0x24
#define KEY_CAPS	0x00

#define KEY_0		0x14
#define KEY_1		0x0f
#define KEY_2		0x10
#define KEY_3		0x11
#define KEY_4		0x12
#define KEY_5		0x13
#define KEY_6		0x18
#define KEY_7		0x17
#define KEY_8		0x16
#define KEY_9		0x15

#define KEY_A		0x05
#define KEY_B		0x27
#define KEY_C		0x03
#define KEY_D		0x07
#define KEY_E		0x0c
#define KEY_F		0x08
#define KEY_G		0x09
#define KEY_H		0x22
#define KEY_I		0x1b
#define KEY_J		0x21
#define KEY_K		0x20
#define KEY_L		0x1f
#define KEY_M		0x25
#define KEY_N		0x26
#define KEY_O		0x1a
#define KEY_P		0x19
#define KEY_Q		0x0a
#define KEY_R		0x0d
#define KEY_S		0x06
#define KEY_T		0x0e
#define KEY_U		0x1c
#define KEY_V		0x04
#define KEY_W		0x0b
#define KEY_X		0x02
#define KEY_Y		0x1d
#define KEY_Z		0x01

//флаги состояния клавиш

#define KEY_DOWN	0x01	//кнопка удерживается
#define KEY_PRESS	0x02	//кнопка нажата, флаг сбрасывается после вызова keyboard

//флаги кнопок мыши

#define MOUSE_LBTN	0x01
#define MOUSE_RBTN	0x02
#define MOUSE_MBTN	0x04

//получение кода цвета из RGB с двухбитными значениями каналов

#define RGB222(r,g,b)	(((b)&3)|(((g)&3)<<2)|(((r)&3)<<4))

//крайние и средний уровни яркости

#define BRIGHT_MIN	0
#define BRIGHT_MID	3
#define BRIGHT_MAX	6

//код конца списка спрайтов

#define SPRITE_END	0xff00



//заполнение памяти заданным значением

void memset(void* m,u8 b,u16 len) _naked;

//копирование памяти, области не должны пересекаться

void memcpy(void* d,void* s,u16 len) _naked;

//генерация 16-битного псевдослучайного числа

u16 rand16(void) _naked;

//установка цвета бордюра, 0..15

void border(u8 n) _naked;

//ожидание следующего ТВ кадра

void vsync(void) _naked;

//опрос kempston джойстика и курсорных клавиш с пробелом
//для опроса кнопок есть константы JOY_

u8 joystick(void) _naked;

#if defined(__SPRINTER__) && (NATIVE || SEGA_EX)
//Sprinter: полный опрос Sega-геймпада (3/6 кнопок), если включён NATIVE или SEGA_EX.
//Возвращает u16: младший байт -- стандартные JOY_RIGHT..JOY_START (направления +
//B(=FIRE),C,A,Start), старший байт -- доп. кнопки 6-button + флаг подключения.
//Активный Sega-пад "дёргает" SEL внутри опроса (9 полуциклов), дороже обычного
//joystick(); направления берутся из надёжного 3-го цикла. Только под Sprinter.
#define JOY_B          0x0010   /* == JOY_FIRE */
#define JOY_C          0x0020
#define JOY_A          0x0040
#define JOY_START      0x0080
#define JOY_MODE       0x0100
#define JOY_X          0x0200
#define JOY_Y          0x0400
#define JOY_Z          0x0800
#define JOY_STAR       0x1000
#define JOY_HOME       0x2000
#define JOY_CONNECTED  0x8000   /* 6-кнопочный пад обнаружен (иначе только 3 кнопки) */

u16 joystick_ex(void) _naked;
#endif

//опрос клавиатуры, возвращает состояние клавиш в 40-байтный массив
//для опроса клавиш есть константы KEY_

void keyboard(u8* keys) _naked;

//получение текущей позиции указателя мыши, разрешение по x понижено вдвое, как для спрайтов
//мышь перемещается в заданной зоне клиппинга

u8 mouse_pos(u8* x,u8* y) _naked;

//установка текущей позиции указателя мыши

void mouse_set(u8 x,u8 y) _naked;

//установка зоны клиппинга для указателя мыши, по умолчанию 0..160, 0..200

void mouse_clip(u8 xmin,u8 ymin,u8 xmax,u8 ymax) _naked;

//получение дельты перемещения мыши, без понижения разрешения по x

u8 mouse_delta(i8* x,i8* y) _naked;

//проигрывание звукового эффекта с указанным номером и относительной громкостью -8..8

void sfx_play(u8 sfx,i8 vol) _naked;

//останов всех проигрываемых звуковых эффектов

void sfx_stop(void) _naked;

//проигрывание музыки с указанным номером

void music_play(u8 mus) _naked;

//прекращение проигрывания музыки

void music_stop(void) _naked;

//проигрывание сэмпла с указанным номером через Covox
//во время проигрывания программа ждёт и прерывания запрещены

void sample_play(u8 sample) _naked;

#if SAMPLE_ASYNC
//Sprinter-only, ВКЛЮЧАЕТСЯ флагом сборки SAMPLE_ASYNC=1 (по умолчанию выключено —
//код не ассемблируется, сборки байт-в-байт неизменны).
//Неблокирующее воспроизведение сэмпла: армит CBL и СРАЗУ возвращается; FIFO
//подкачивается в фоне из IM2-обработчика #FF, игра и музыка продолжают идти.
//По завершении сэмпла обработчик сам переводит CBL в idle. Новый вызов во время
//активного сэмпла перезапускает воспроизведение (последний побеждает).
//ВНИМАНИЕ: при SAMPLE_ASYNC=1 окна DI в горячих циклах вывода (спрайты, clear_screen,
//tile-restore) сужаются до отдельных accel-операций, чтобы refill-IRQ не голодал —
//это чуть дороже по тактам на операцию, но позволяет играть сэмплы без пропусков.

void sample_play_async(u8 sample) _naked;
#endif

//установка всех значений в палитре в 0 (чёрный цвет)

void pal_clear(void) _naked;

//установка яркости экрана BRIGHT_MIN..BRIGHT_MID..BRIGHT_MAX (0..3..6)
//от полностью чёрного экрана до нормальной яркости до полностью белого экрана

void pal_bright(u8 bright) _naked;

//Sprinter-расширение: тонкая яркость level=0..32 (32=норма, 0=чёрный). Даёт 32
//линейных шага в диапазоне затемнения вместо 4 у pal_bright(0..6) — для плавного
//фейда 256-цветных изображений без бандинга. Только под __SPRINTER__.

void pal_bright_fine(u8 level) _naked;

//выбор предопределённой палитры по номеру.
//Sprinter: если ассет 16-цветный — пишутся аппаратные индексы 0..15 (R2G2B2,
//путь EvoSDK). Если ассет 256-цветный — пишутся ВСЕ 256 записей с полной
//точностью 8 бит/канал. Режим запоминается, поэтому последующие pal_bright/
//pal_bright_fine корректно перечитывают и масштабируют тот же ассет.

void pal_select(u8 id) _naked;

//копирование предопределённой палитры в массив RGB222[16] (16 байт).
//Sprinter: только нижние 16 записей; 256-цветный ассет усекается до первых 16
//цветов, понижая каждый канал до 2 бит (RGB222). Для полноточностной работы с
//256-цветной палитрой использовать pal_load256/pal_set256.

void pal_copy(u8 id,u8* pal) _naked;

//установка цвета в палитре, id 0..15, col в формате R2G2B2.
//Sprinter: 16-цветный путь — обновляет ТОЛЬКО аппаратные индексы 0..15 и в 2-бит
//точности; не достаёт до индексов 16..255. После вызова pal_bright re-apply идёт
//по 16-записному пути. Для 256-цветной палитры использовать pal_set256.

void pal_col(u8 id,u8 col) _naked;

//установка всех 16 цветов в палитре значениями R2G2B2 из массива (индексы 0..15).
//Sprinter: см. pal_col — затрагивает только индексы 0..15.

void pal_custom(u8* pal) _naked;

#ifdef __SPRINTER__
//Sprinter-расширение (только __SPRINTER__): полноточностный 256-цветный рантайм-API.
//Палитра Sprinter — 8 бит/канал (24-битный цвет), а pal_col/pal_custom дают лишь
//RGB222 и только нижние 16 индексов. Эти функции пишут любые из 256 записей с
//полной 8-битной точностью каждого канала.

//загрузка полной 256-цветной палитры из памяти C: pal указывает на 256*(R8,G8,B8)
//= 768 байт (БЕЗ префикса count). Применяется немедленно на ТЕКУЩЕМ коэрцивном
//(coarse) уровне яркости pal_bright — так же, как pal_select. После транзитного
//pal_bright_fine-фейда выставь pal_bright() перед загрузкой, иначе палитра ляжет
//на устаревший coarse-уровень (напр. BRIGHT_MIN → чёрный). Указатель запоминается,
//поэтому последующие pal_bright/pal_bright_fine перечитывают этот же буфер.

void pal_load256(const void* pal) _naked;

//установка одной записи 256-цветной палитры с полной 8-битной точностью.
//index 0..255, r/g/b 0..255. Канал проходит через текущую кривую яркости, поэтому
//запись совпадает с активным фейдом. Не меняет 16/256-режим. Предусловие: активна
//256-цветная палитра (pal_lut8 построен последним pal_select/pal_load256/pal_bright).
//Рантайм-правки перекрываются следующим pal_select/pal_load256/pal_bright[_fine].
//Для тинта/color-cycling вызывать после установки базовой палитры.

void pal_set256(u8 index,u8 r,u8 g,u8 b) _naked;
#endif

//выбор изображения для функций draw_tile

void select_image(u8 id) _naked;

//выбор прозрачного цвета 0..15 для функции draw_tile_key

void color_key(u8 col) _naked;

//отрисовка тайла из текущего выбранного изображения
//в одном изображении может быть до 65536 тайлов

void draw_tile(u8 x,u8 y,u16 tile) _naked;

//отрисовка тайла с маской, заданной номером прозрачного цвета

void draw_tile_key(u8 x,u8 y,u16 tile) _naked;

//отрисовка изображения целиком

void draw_image(u8 x,u8 y,u8 id) _naked;

//очистка теневого экрана нужным цветом 0..15

void clear_screen(u8 color) _naked;

//переключение экранов, теневой становится видимым
//ожидание кадра выполняется автоматически, vsync перед вызовом этой функции не нужен
//функция также обновляет спрайты, если они включены

void swap_screen(void) _naked;

//запуск системы вывода спрайтов
//на видимом экране должно быть изображение, поверх которого будут выведены спрайты
//эта функция выполняется медленно, происходит копирование большого объёма данных
//после того как спрайты разрешены, они будут автоматически выводиться при swap_screen

void sprites_start(void) _naked;

//останов системы вывода спрайтов

void sprites_stop(void) _naked;

//установка положения спрайта
//id номер в списке 0..63
//x координата: режим совместимости (по умолчанию) — 0..152, точность 2 пикселя,
//   как в EvoSDK; нативный режим Sprinter (сборка NATIVE=1) — 0..319 попиксельно
//y координата 0..184 (совместимость) / 0..240 (нативный, экран 320×256)
//spr номер изображения спрайта, если SPRITE_END, то вывод списка прекращается
//внимание: клиппинг при выводе отсутствует, спрайт не может частично заходить
//за границу экрана
//ВНИМАНИЕ (Sprinter): x — 16-битный (u16), чтобы покрыть всю ширину в нативном
//режиме; в режиме совместимости значения 0..152 работают как раньше. На Evo x — u8.

void set_sprite(u8 id,u16 x,u8 y,u16 spr) _naked;

#if SPRITE16
//Sprinter-only, ВКЛЮЧАЕТСЯ флагом сборки SPRITE16=1 (по умолчанию выключено —
//код не ассемблируется, существующие сборки байт-в-байт неизменны).
//Immediate-mode масочный accel-блит одной ячейки 16×16 ПРЯМО в теневой буфер по
//пиксельным координатам — БЕЗ слота списка спрайтов и БЕЗ лимита 64. В отличие от
//set_sprite фон НЕ восстанавливается автоматически: вызывающий сам перерисовывает
//его каждый кадр (гибридный рендер). Рисует в тот же теневой буфер, что draw_tile/
//draw_image, поэтому ложится поверх нарисованного. Для каскадов/полей >64 ячеек.
//x 0..304 (пиксели), y 0..240; полностью за экраном (x≥305 / y+16>256) — пропуск,
//как у списка. spr 0..255. flags бит0: 0 = прозрачно (индекс 255), 1 = непрозрачно.
//Чистый режим: НЕ вызывать sprites_start() — тогда swap_screen просто делает флип.

void draw_sprite16(u16 x,u8 y,u16 spr,u8 flags) _naked;
#endif

//время с момента запуска программы в кадрах

u32 time(void) _naked;

//задержка, значение в кадрах (1/50 секунды)

void delay(u16 time) _naked;

#ifdef __SPRINTER__
//Sprinter-only: возврат управления в DSS (выход из программы).
//На Evo-таргете отсутствует -- расширение специфики платформы (CLAUDE.md §2.2).
//Не возвращается.

void quit_to_dss(void) _naked;
#endif
