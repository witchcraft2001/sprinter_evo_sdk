#include <evo.h>

/*
 * Sprinter currently exports the EvoSDK C ABI entry points directly from the
 * asm libraries. This file keeps the SDK file map aligned with evosdk/evo.c
 * without adding duplicate wrappers.
 */
