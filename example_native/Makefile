PROJECT := .

# Native Sprinter screen mode: full 320x256 surface + per-pixel sprite X (0..319).
# This demo is authored for native, so build it native by default. Existing EvoSDK
# projects keep NATIVE=0 (compatibility) -- see plan.md §9.2 / CLAUDE.md §2.
NATIVE := 1

all: exe

include ../sprintersdk/sprinter.mk
