# Неточности мануала Sprinter (CBL/COVOX, Kempston-джойстик)

Каталог расхождений мануала с тем, что выяснилось на **реальном железе**: разделы
1–7 — про вывод звука через CBL (`sprinter_ai_doc/manual/06_audio/02_covox.md`,
`03_covox_dma.md`); раздел 8 — про порт Kempston-джойстика. Каждая запись: где, в чём
проблема, как на самом деле, источник, чем грозит.

Источники истины:
- **FPGA:** `sprinter_bios/src/altera/quartus/acex/SP2_ACEX.TDF` (узлы `CBL_*`) и `DCP.TDF`
  (ремаппинг портов).
- **Эмулятор:** MAME `src/mame/sinclair/sprinter.cpp` (holub/mame).
- **Рабочие плееры:** `sources/mkdemo/src/mk_common.asm` (MK_DEMO/MK_OUTI),
  `sources/modplay/refs/wavplay/` (WAVPLAY).
- **Реализация порта:** `sprintersdk/lib_sound.asm` (`_evo_cbl_irq`, `sample_play`),
  `sprintersdk/tools/assetpack.py` (`cbl_ctrl_for_rate`).
- **Подробный разбор эмулятор↔железо:** `cbl_issue_mame.md`.

---

## 1. Порты CBL: мануал даёт внутренние `#88/#89`, умалчивает о DCP-ремаппинге `#4E/#4F` (КРИТИЧНО)

**Где:** 02_covox.md, «Порты COVOX» (`#88 CBL_WR`, `#89 CBL_MODE`) и врезка:
> «**Внимание:** порт `#89` используется ещё и для установки Y-координаты палитры/строки в
> видео. Это не конфликт: FPGA различает их по значениям.»
Также 03_covox_dma.md `InitCovoxDMA: out (#89),a`.

**Проблема.** `#88/#89` — это **внутренние** (пост-DCP) порты CBL. Реальные плееры (MK_OUTI,
WAVPLAY) и рабочий SDK пишут CBL по портам **`#4E` (control) / `#4F` (data)**, которые DCP
ремаппит во внутренние `#88/#89`. Мануал нигде не упоминает DCP-ремаппинг и подаёт `#88/#89`
как порты для прямой записи. При этом `#89` — это **живой видеорегистр `PORT_VCONFIG`/PORT_Y**,
который графический код дёргает каждый цикл вывода. Утверждение «не конфликт, различает по
значениям» — **неверно и опасно**: прямая запись CBL-конфига в `#89` затирает PORT_Y и ломает
видео (лаги/пропуск кадров — проверено на практике при попытке писать CBL в `#89`).

**Как на самом деле.** Программа пишет CBL через `#4E/#4F`; DCP (`DCP.TDF`) ремаппит их во
внутренний CBL-блок, который в `SP2_ACEX.TDF` декодируется как `#88/#89`. И MAME, и железо
принимают `#4E/#4F`. Прямую запись в `#89` для CBL делать нельзя — это видео-регистр.

**Чем грозит.** Следуя мануалу буквально (`out (#89)` для режима), можно сломать видео и/или
не получить звука. Это стоило заметного времени отладки.

---

## 2. Контрол-байт: примеры без `INT_ENA`, delay-вывод подан как рабочий (КРИТИЧНО для железа)

**Где:** 02_covox.md, примеры `#81 (on+22kHz)`, `#8C (mono 31.25kHz)` — **бит4 INT_ENA не
установлен**; раздел «Воспроизведение буфера через задержки» с delay-loop.

**Проблема.** На реальном железе **надёжная подача требует прерывания** (`INT_ENA=1`, бит4) —
delay-loop и поллинг-без-прерывания не держат синхронизацию (FIFO underrun → хрип/растяжка).
Мануал подаёт режимы без INT и delay-вывод как нормальные варианты, лишь оговаривая, что
delay «требует точного таймирования».

**Как на самом деле.** Для PCM нужно `INT_ENA=1` и подача из обработчика прерывания CBL.
Это путь MK_OUTI/WAVPLAY и SDK (`_evo_cbl_irq`). Delay/поллинг — нежизнеспособны на железе.

**Чем грозит.** Код по примерам мануала «играет» в MAME и хрипит на железе.

---

## 3. Контрол-байт: не предупреждено, что STEREO/16-бит должны соответствовать формату данных

**Где:** 02_covox.md, регистр `#89` (биты 6 STEREO, 5 16-бит) и пример `#FD`.

**Проблема.** Если выставить STEREO/16-бит для моно-8-битных данных, сэмпл читается как
стерео-16 (4 байта на выходной сэмпл) → искажение и неверная длительность. У нас именно так
и было: байт `#D9` (= STEREO+16-бит) для моно-8-бит сэмплов давал скрежет; чинилось на
`#99/#9A/#9B` (моно 8-бит). MAME это маскирует мягче, чем железо. Мануал про эту ловушку молчит.

**Как на самом деле.** Биты 6/5 обязаны соответствовать формату PCM. Моно 8-бит → `0x90 | rate`
(`#99/#9A/#9B/#9C` для 11/15.6/21.9/31.25 кГц). См. `assetpack.cbl_ctrl_for_rate`.

---

## 4. `#FE.bit7` (half-FIFO индикатор) не задокументирован; нет различения источника прерывания

**Где:** 02_covox.md, «Прерывания от COVOX»:
> «В обработчике прерываний IM2: определить, что это CBL (другие источники возможны); записать
> новые сэмплы в #88.»

**Проблема.** Не сказано **как** определить, что прерывание от CBL, и что у CBL **нет своего
вектора** — он делит байт вектора `#FF` с видео-IRQ. Ключевой бит `#FE.bit7 = CBL_IND =
CBL_CNT7 xor CBL_WA7` (индикатор половины буфера) в разделе CBL вообще не упоминается.

**Как на самом деле.** В обработчике на `…FF` читать `#FE.bit7`: установлен → событие CBL →
долить; сброшен → видеокадр → выйти. `SP2_ACEX.TDF`: `CBL_IND = CBL_CNT7 xor CBL_WA7`,
`#FE.bit7 = CBL_IND & CBL_MODE`. Так делают MK_OUTI/WAVPLAY и SDK (`_evo_cbl_irq`).

---

## 5. КРИТИЧЕСКОЕ расхождение эмулятор↔железо по `#FE.bit7` не отмечено

**Где:** весь раздел CBL мануала.

**Проблема.** Состояние «половина опустела» **читается** в `#FE.bit7` (в т.ч. поллингом), но
на реальной FPGA **надёжная синхронизация дозаполнения достигается только обслуживанием
прерывания `CBL_INT`** — выравнивание указателя записи `CBL_WA` привязано к `CBL_INT`. Свободный
поллинг рассинхронизируется на железе, **хотя в MAME проходит** (там `CBL_WA` выравнивается
безусловно в аудио-таймере). Мануал об этом не предупреждает — а это главная ловушка
«работает в эмуляторе, не работает на железе».

**Как на самом деле / источник.** Подробно и с цитатами обоих исходников (MAME `cbl_tick()`/
`kbd_fe_r()` vs `SP2_ACEX.TDF` `CBL_INT`/`CBL_WA`) — в `cbl_issue_mame.md`. Практический вывод:
кормить FIFO из IM2-обработчика, не свободным опросом.

---

## 6. DMA-режим: устаревший хедж «требует знание FPGA 3.x / детали сложны»

**Где:** 03_covox_dma.md:
> «Детали механизма сложны и выходят за рамки простого примера…»
> «Для полноценной работы DMA требуется знание внутренней архитектуры FPGA 3.x…»

**Проблема.** Механизм полностью выводится из `SP2_ACEX.TDF`: `CBL_WR` срабатывает при
**accel-записи в страницу `#FD`** при `CBL_INT_ENA & ACC.ACC_DIR1`
(`DECODE.PAGE==B"11111101"`). То есть DMA = акселераторная блок-запись в `#FD`, синхронизируемая
тем же `CBL_INT`. Это рабочий путь MK_DEMO (`video_irq`: `LD D,D / LD L,L` — accel — в `#FD`).
Хедж создаёт ложное впечатление, что DMA недокументируем.

**Как на самом деле.** DMA-подача: замапить страницу `#FD` в окно, accel-копировать 128
сэмплов в неиграющую половину; пейсинг — по `CBL_INT`. Накладные ниже, чем у `OUT #4F` по байту.

---

## 7. Пример IM2-стриминга в 03 — неполный/сбивающий

**Где:** 03_covox_dma.md, `InitStreamingAudio`/`CovoxISR` (вектор `#80FF`, запись в `#88`).

**Проблема.** ISR пишет в `#88` по байту в `djnz`-цикле (256 раз) на каждом прерывании, но:
(а) не упоминает различение по `#FE.bit7`; (б) использует частный вектор `#80FF` без пояснения,
что CBL делит `#FF` с видео; (в) не показывает связь с `CBL_INT`/half-FIFO. Подаётся как
«переносимый» путь, хотя без проверки `#FE.bit7` он не синхронизирован.

**Как на самом деле.** См. п.4/п.5: проверять `#FE.bit7`, долив 128 (а не 256) на половину,
пейсинг по `CBL_INT`.

---

## 8. Kempston-джойстик: порт `#07` из SRAM-кэша vs `#1F`, и дёрганье SEL (КРИТИЧНО)

**Где:** `09_advanced/04_zx_compat.md` («Kempston Joystick»: *«В Sprinter этот порт [`#1F`]
не занят физическим джойстиком, но может эмулироваться…»*) и `01_architecture/04_port_map.md`
(`16h..1Fh — reserved`). Мануал подаёт `#1F` как неиспользуемый/неясный и не описывает
реальную работу с джойстиком.

**Проблема.** На плате есть полноценный вход джойстика (разъём DB-9 `X30`, Sega-совместимый),
но:
1. **`#1F` — это одновременно внутренний PIO Z84C15** (диапазон `#10–#1F`, port B). Код,
   исполняемый из **SRAM-кэша (WIN0)**, при `IN A,(#1F)` читает **внутренний порт CPU**, а не
   джойстик. Мануал об этом молчит.
2. **Из кэша джойстик доступен через порт `#07`** (а из обычной памяти — через `#1F`). Это
   нигде не задокументировано.
3. **Форма инструкции важна:** `IN A,(n)` (DB-опкод) перенаправляется прошивкой во внешний
   декод, а `IN A,(C)` (ED-опкод, `LD BC,#1F`) — **не** перенаправляется и попадает во
   внутренний PIO. Программы на `IN A,(C)` «не видят» джойстик (это прямо отмечено в
   `sp2000.pdf §9`, но не в основном мануале).
4. **Активному Sega-геймпаду нужно «дёргать» SEL** перед каждым чтением (статический `IN`
   даёт устаревшие данные). Не задокументировано.

**Как на самом деле.**
- Декодер Kempston **не использует биты адреса A3 и A4** (индекс DCP = `A2,A5,A6,A7,A13,A14,A15`,
  `DCP.TDF:349`; в `sp2000.pdf` — «биты 0,1,2,5,6,7,13,14,15»). Поэтому `#03/#07/#0B/#0F/#13/
  #17/#1B/#1F` — **один и тот же** внешний порт; `#07` — алиас `#1F` вне диапазона внутренней
  периферии CPU.
- Из SRAM-кэша читать `#07` (мы держим `A=#E0` → `#E007`), иначе (код ≥#4000) — `#1F`.
- SEL = бит 7 (DTR) регистра WR5 SIO канала B (порт `#1B`); `#E0`=high, `#60`=low. Направления
  надёжны на **3-м полуцикле** последовательности High→Low→High (1-й цикл на 8BitDo M30 и т.п.
  отдаёт мусор). Полный 3/6-кнопочный опрос — `sprinterJoySegaLib.asm` / `SJTEST.ASM`.

**Источник:** `sp2000.pdf §9` («Внутренние порты Z84C15»: конфликт `#1F`/ВГ93/джойстик,
перенаправление `#1F→#0F`, `IN A,(C)` бьёт внутренний порт); `SP2_ACEX.TDF` (`RD_KMPS =
…TYPE==7`); `DCP.TDF:349` (индекс без A3/A4); схема `board/.../sp-dx-sch.pdf` лист 7 (`X30`,
`/DTRB→J_SEL→74HC257`); реализация — `sprintersdk/lib_input.asm` (`joystick`/`joystick_ex`).

**Чем грозит.** Чтение `#1F` из кэша возвращает внутренний PIO CPU (мусор) вместо джойстика;
статическое чтение активного Sega-пада даёт устаревшее состояние. Симптом — «джойстик не
работает», хотя порт/инструкция кажутся верными. Стоило существенного времени отладки.

---

## Сводка приоритетов

| # | Тема | Серьёзность |
|---|------|-------------|
| 1 | `#4E/#4F` vs `#88/#89` + DCP; «`#89` не конфликт» неверно | критично |
| 5 | поллинг `#FE.bit7` работает в MAME, не на железе | критично |
| 2 | нужен `INT_ENA` + прерывание; delay/поллинг нежизнеспособны | критично |
| 4 | `#FE.bit7`/`CBL_IND` и общий вектор `#FF` не описаны | важно |
| 3 | STEREO/16-бит ↔ формат данных (ловушка `#D9`) | важно |
| 6 | устаревший хедж про «FPGA 3.x» в DMA | средне |
| 7 | пример IM2-стриминга неполон | средне |
| 8 | Kempston: `#07` из кэша / `#1F` иначе; `#1F`=внутр. PIO; `IN A,(C)`≠джойстик; SEL-дёрганье | критично |

Предлагаемый текст-врезка для исправления §6.2 — см. обсуждение/`cbl_issue_mame.md`.
